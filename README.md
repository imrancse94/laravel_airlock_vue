Laravel Airlock With VueJS authentication.
This is the cookie base session auth which is more secured. No need to store token in frontEnd 
(VueJS). But need to keep frontEnd & Backend in same domain.
